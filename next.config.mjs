/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      appDir: true, // Habilita el uso de App Router (si aplicable)
    },
    reactStrictMode: true, // Asegura buenas prácticas en React
  };
  
  export default nextConfig;
  